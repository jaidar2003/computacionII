# Dependencias básicas
socket
ssl
threading
argparse
logging
hashlib
sqlite3

# Dependencias para tareas asíncronas
celery>=5.2.7
redis>=4.3.4

# Dependencias para operaciones asíncronas de I/O
asyncio>=3.4.3
aiofiles>=0.8.0

# Dependencias para cifrado
cryptography>=37.0.4

# Dependencias opcionales
# Para interfaz web (si se implementa en el futuro)
# flask>=2.1.2
# Para despliegue en contenedores (si se implementa en el futuro)
# docker>=5.0.3